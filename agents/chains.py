
if "blah" not in somestring: 
    continue

if horizontal:
	return max

elif vertical:
	return max
------------------------------
            r, c, o = free_lines[movei]


used_lines = self.board.used_lines()


longest chain { 
	if not present: continue
		chain++
		max (longest chain) 
	else: break

--> houdt alle bezochten lijnen bij. Break vanaf al bezochtj
Vind_ketting(line){

if horizontal:
	connected = true
elif vertical:
	connected = true
else:
	connected = false


	if ketting & connected = true{
		count++
		if count >= 3 then chain ++ 
		vind_ketting(line)

	if ketting{
		count++
		if count >= 3 then chain ++ 
		vind_ketting()
	else{
		connected = false
}
